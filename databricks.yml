bundle:
  name: dbx_app
  uuid: eb6c7e00-3392-4409-b933-2900e33b5b16

include:
  - resources/*.yml
  - resources/*/*.yml

variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405608824163183.3.azuredatabricks.net
    variables:
      catalog: dbx_catalog
      schema: ${workspace.current_user.short_name}

  prod:
    mode: production
    workspace:
      host: https://adb-7405604989688915.15.azuredatabricks.net
      root_path: /Workspace/Users/diogo.duarte@xpandtst.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dbx_catalog
      schema: prod
    permissions:
      - user_name: diogo.duarte@xpandtst.onmicrosoft.com
        level: CAN_MANAGE
